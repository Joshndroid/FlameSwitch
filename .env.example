PORT=5005
NODE_ENV=development
PASSWORD=change-me
# Leave SECRET unset to generate and persist a random key in data/secret.key.
SECRET=CHANGE_ME
WEATHER_CACHE_HOURS=1
# Use true for one trusted reverse proxy, or a positive number for that many hops.
TRUST_PROXY=false
