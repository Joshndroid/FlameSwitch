PORT=5005
NODE_ENV=development
PASSWORD=change-me
# Leave SECRET unset to generate and persist a random key in data/secret.key.
SECRET=CHANGE_ME
WEATHER_CACHE_HOURS=1
# Set to true only when the app is behind one trusted reverse proxy.
TRUST_PROXY=false
